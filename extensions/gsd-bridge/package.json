{
  "name": "@openclaw/gsd-bridge",
  "version": "0.1.0",
  "description": "OpenClaw native plugin — GSD planning bridge and LOOP-QUEUE execution",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "build:watch": "tsc --project tsconfig.json --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=24.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "typescript": "^5.7.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "activation": {
      "onStartup": true
    }
  },
  "dependencies": {
    "openclaw": "file:/home/korben/.npm-global/lib/node_modules/openclaw"
  }
}
